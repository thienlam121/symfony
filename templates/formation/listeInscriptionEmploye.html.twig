{% block body %}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <img src="https://static.vecteezy.com/ti/vecteur-libre/p3/11405698-creation-de-logo-de-formation-en-genie-industriel-modele-de-logo-d-engrenage-etudiant-vectoriel.jpg" alt="Logo" height="30" class="d-inline-block align-text-top">
        <a class="navbar-brand">Tech formation</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{path('app_all_inscription_employe')}}">Mes inscriptions</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('app_all_formation_employe')}}">Les formations</a>
                </li>
            </ul>

            <a class="btn btn-outline-danger" href="{{ path('app_login') }}">Déconnexion</a>
        </div>
    </div>
</nav>

<div class="container">
    <div class="d-flex justify-content-between align-items-center">
        <h1 class="mt-5 mb-4">Liste des formations</h1>
    </div>
    <div class="row">
        {% for uneInscription in inscriptions %}
        <div class="col-md-4">
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">Nom de la formation : {{uneInscription.formation.nom}}</h5>
                    <p class="card-text">ID de la formation : {{uneInscription.formation.id}}</p>
                    <p class="card-text">ID de l'inscription : {{uneInscription.id}}</p>
                    <p class="card-text">Nom du produit : {{uneInscription.formation.produit.libelle}}</p>
                    <p class="card-text">Statut de l'inscription : {{uneInscription.statut}}</p>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
